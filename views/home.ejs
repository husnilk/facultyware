<!doctype html>
<html lang="id">
  <head>
    <script>
      (() => {
        try {
          const stored = localStorage.getItem("themeMode");
          if (
            stored
              ? stored === "dark"
              : matchMedia("(prefers-color-scheme: dark)").matches
          ) {
            document.documentElement.classList.add("dark");
          }
        } catch (_) {}

        const apply = (dark) => {
          document.documentElement.classList.toggle("dark", dark);
          try {
            localStorage.setItem("themeMode", dark ? "dark" : "light");
          } catch (_) {}
        };

        document.addEventListener("basecoat:theme", (event) => {
          const mode = event.detail?.mode;
          apply(
            mode === "dark"
              ? true
              : mode === "light"
                ? false
                : !document.documentElement.classList.contains("dark"),
          );
        });
      })();
    </script>

    <script>
      (() => {
        try {
          const storedTheme = localStorage.getItem("themeVariant");
          if (storedTheme) {
            document.documentElement.classList.add(`theme-${storedTheme}`);
          }
        } catch (event) {
          console.error("Could not apply theme variant from localStorage", event);
        }
      })();
    </script>

    <meta charset="utf-8" />
    <meta http-equiv="Content-Language" content="id" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <title>Dashboard - FTI Event Management</title>

    <link rel="icon" type="image/svg+xml" href="/assets/favicon.svg" />
    <link rel="stylesheet" href="/assets/styles.css" />

    <script src="/assets/js/basecoat.js?v=0.3.11" defer></script>
    <script src="/assets/js/command.js?v=0.3.11" defer></script>
    <script src="/assets/js/dropdown-menu.js?v=0.3.11" defer></script>
    <script src="/assets/js/popover.js?v=0.3.11" defer></script>
    <script src="/assets/js/select.js?v=0.3.11" defer></script>
    <script src="/assets/js/sidebar.js?v=0.3.11" defer></script>
    <script src="/assets/js/tabs.js?v=0.3.11" defer></script>
    <script src="/assets/js/toast.js?v=0.3.11" defer></script>

    <style>
      .dashboard-wrapper {
        max-width: 1180px;
        margin: 0 auto;
      }

      .dashboard-header {
        margin-bottom: 1.75rem;
      }

      .dashboard-title {
        font-size: 2.125rem;
        font-weight: 800;
        letter-spacing: -0.03em;
        line-height: 1.15;
        margin-bottom: 0.5rem;
      }

      .dashboard-subtitle {
        font-size: 1rem;
        line-height: 1.75;
        max-width: 950px;
      }

      .dashboard-grid {
        display: grid;
        grid-template-columns: repeat(3, minmax(0, 1fr));
        gap: 1rem;
      }

      .dashboard-card {
        min-height: 190px;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
      }

      .dashboard-card h2 {
        font-size: 1.075rem;
        font-weight: 700;
      }

      .dashboard-card p {
        font-size: 0.9rem;
        line-height: 1.65;
      }

      .card-footer-actions {
        display: flex;
        flex-wrap: wrap;
        gap: 0.5rem;
        margin-top: 1rem;
      }

      .flow-card {
        margin-top: 1.25rem;
      }

      .flow-list {
        display: grid;
        gap: 0.75rem;
        margin-top: 0.75rem;
      }

      .flow-item {
        display: flex;
        gap: 0.75rem;
        align-items: flex-start;
        font-size: 0.9rem;
        line-height: 1.6;
      }

      .flow-number {
        min-width: 1.65rem;
        height: 1.65rem;
        border-radius: 999px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 0.75rem;
        font-weight: 700;
      }

      @media (max-width: 1100px) {
        .dashboard-grid {
          grid-template-columns: repeat(2, minmax(0, 1fr));
        }
      }

      @media (max-width: 700px) {
        .dashboard-grid {
          grid-template-columns: 1fr;
        }

        .dashboard-title {
          font-size: 1.75rem;
        }
      }
    </style>

    <script>
      document.addEventListener("DOMContentLoaded", () => {
        const currentPath = window.location.pathname.replace(/\/$/, "");

        document.querySelectorAll("#sidebar a").forEach((link) => {
          const href = link.getAttribute("href");
          if (!href || href.startsWith("http")) return;

          const linkPath = new URL(href, window.location.origin)
            .pathname.replace(/\/$/, "");

          if (linkPath === currentPath) {
            link.setAttribute("aria-current", "page");
          } else {
            link.removeAttribute("aria-current");
          }
        });
      });
    </script>
  </head>

  <body class="bg-background text-foreground">
    <aside id="sidebar" class="sidebar" data-side="left" aria-hidden="false">
  <nav aria-label="Sidebar navigation">
    <header>
      <a href="/home" class="btn-ghost p-2 h-12 w-full justify-start">
        <div
          class="bg-sidebar-primary text-sidebar-primary-foreground flex aspect-square size-8 items-center justify-center rounded-lg font-bold"
        >
          F
        </div>

        <div class="grid flex-1 text-left text-sm leading-tight">
          <span class="truncate font-medium">FTI Event</span>
          <span class="truncate text-xs">Management System</span>
        </div>
      </a>
    </header>

    <%
      const hasAccess = (requiredPermission) => {
        if (role === 'admin') return true;
        if (!requiredPermission) return true;
        if (Array.isArray(requiredPermission)) {
          return requiredPermission.some(p => permissions.includes(p));
        }
        return permissions.includes(requiredPermission);
      };

      const renderSidebarLink = (href, text, requiredPermission) => {
        const isAllowed = hasAccess(requiredPermission);
        const isPesertaRole = role === 'peserta';

        if (isAllowed) {
    %>
          <li>
            <a href="<%= href %>">
              <span><%= text %></span>
            </a>
          </li>
    <%
        } else {
          if (!isPesertaRole) {
    %>
            <li class="restricted" style="opacity: 0.55; cursor: not-allowed; display: flex; align-items: center; justify-content: space-between; padding: 6px 12px; pointer-events: none; width: 100%;">
              <span style="color: #94a3b8;"><%= text %></span>
              <span style="font-size: 8px; font-weight: 700; padding: 1px 4.5px; background: #fee2e2; color: #ef4444; border-radius: 3px; border: 1px solid #fca5a5;">Locked</span>
            </li>
    <%
          }
        }
      };

      const hasSectionAccess = (requiredPermissions) => {
        if (role === 'admin') return true;
        if (role !== 'peserta') return true; // panitia sees locked items anyway
        return requiredPermissions.some(p => hasAccess(p));
      };
    %>

    <section class="scrollbar">
      <% if (hasSectionAccess(['view_dashboard'])) { %>
        <div role="group">
          <h3>Dashboard</h3>
          <ul>
            <% renderSidebarLink('/home', 'Dashboard Utama', 'view_dashboard') %>
          </ul>
        </div>
      <% } %>

      <% if (hasSectionAccess(['manage_events', 'manage_committee'])) { %>
        <div role="group">
          <h3>Event & Panitia</h3>
          <ul>
            <% renderSidebarLink('/admin/events', 'Manajemen Event', 'manage_events') %>
            <% renderSidebarLink('/admin/events', 'Tambah Event', 'manage_events') %>
            <% renderSidebarLink('/admin/events', 'Publish Event', 'manage_events') %>
            <% renderSidebarLink('/admin/events', 'Kelola Panitia', 'manage_committee') %>
            <% renderSidebarLink('/admin/events', 'Export Panitia', 'manage_committee') %>
          </ul>
        </div>
      <% } %>

      <% if (hasSectionAccess(['view_events', 'register_events', 'view_registrations', 'view_own_tickets', 'view_tickets'])) { %>
        <div role="group">
          <h3>Registrasi & Tiket</h3>
          <ul>
            <% renderSidebarLink('/events', 'Daftar Event', 'view_events') %>
            <% renderSidebarLink('/registrations', 'Registrasi Peserta', 'register_events') %>
            <% renderSidebarLink('/registrations', 'Detail Registrasi', ['view_registrations', 'view_own_tickets']) %>
            <% renderSidebarLink('/registrations', 'Akses E-Ticket', ['view_tickets', 'view_own_tickets']) %>
            <% renderSidebarLink('/registrations', 'Detail Tiket Peserta', ['view_tickets', 'view_own_tickets']) %>
          </ul>
        </div>
      <% } %>

      <% if (hasSectionAccess(['manage_reminders', 'checkin_participants', 'view_attendance', 'export_attendance'])) { %>
        <div role="group">
          <h3>Reminder & Attendance</h3>
          <ul>
            <% renderSidebarLink('/reminders', 'Kirim Reminder', 'manage_reminders') %>
            <% renderSidebarLink('/reminders/history', 'Riwayat Reminder', 'manage_reminders') %>
            <% renderSidebarLink('/checkins', 'Verifikasi Tiket', 'checkin_participants') %>
            <% renderSidebarLink('/checkins', 'Check-in Peserta', 'checkin_participants') %>
            <% renderSidebarLink('/attendances', 'Attendance List', 'view_attendance') %>
            <% renderSidebarLink('/attendances/recap', 'Rekap Attendance', 'view_attendance') %>
            <% renderSidebarLink('/attendances/export/excel', 'Export Attendance Excel', 'export_attendance') %>
          </ul>
        </div>
      <% } %>

      <% if (hasSectionAccess(['manage_participants', 'view_participants', 'manage_certificates', 'view_certificates', 'view_own_certificates', 'preview_own_certificates'])) { %>
        <div role="group">
          <h3>Peserta & Sertifikat</h3>
          <ul>
            <% renderSidebarLink('/participants', 'Data Peserta', ['manage_participants', 'view_participants']) %>
            <% renderSidebarLink('/participants', 'Detail Peserta', ['manage_participants', 'view_participants']) %>
            <% renderSidebarLink('/certificates', 'Data Sertifikat', ['manage_certificates', 'view_certificates', 'view_own_certificates']) %>
            <% renderSidebarLink('/certificates', 'Eligible Sertifikat', ['manage_certificates', 'view_certificates']) %>
            <% renderSidebarLink('/certificates', 'Lihat Sertifikat', ['manage_certificates', 'view_certificates', 'preview_own_certificates']) %>
          </ul>
        </div>
      <% } %>

      <% if (hasSectionAccess(['manage_reports', 'view_reports'])) { %>
        <div role="group">
          <h3>Laporan & Dokumen</h3>
          <ul>
            <% renderSidebarLink('/reports', 'Laporan Event', ['manage_reports', 'view_reports']) %>
            <% renderSidebarLink('/reports/upload', 'Upload Laporan', 'manage_reports') %>
            <% renderSidebarLink('/reports', 'Dokumen Event', ['manage_reports', 'view_reports']) %>
          </ul>
        </div>
      <% } %>
    </section>

    <footer>
      <a href="/logout" class="btn-ghost p-2 h-12 w-full justify-start">
        <div class="grid flex-1 text-left text-sm leading-tight">
          <span class="truncate font-medium">
            <%= typeof user !== "undefined" && user ? user : "Admin" %>
          </span>
          <span class="truncate text-xs">Logout</span>
        </div>
      </a>
    </footer>
  </nav>
</aside>

    <main id="content">
      <header
        class="bg-background sticky inset-x-0 top-0 isolate flex shrink-0 items-center gap-2 border-b z-10"
      >
        <div class="flex h-14 w-full items-center gap-2 px-4">
          <button
            type="button"
            onclick="document.dispatchEvent(new CustomEvent('basecoat:sidebar'))"
            aria-label="Toggle sidebar"
            data-tooltip="Toggle sidebar"
            data-side="bottom"
            data-align="start"
            class="btn-sm-icon-ghost mr-auto size-7 -ml-1.5"
          >
            <svg
              xmlns="http://www.w3.org/2000/svg"
              width="24"
              height="24"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
            >
              <rect width="18" height="18" x="3" y="3" rx="2" />
              <path d="M9 3v18" />
            </svg>
          </button>

          <select class="select h-8 leading-none" id="theme-select">
            <option value="">Default</option>
            <option value="claude">Claude</option>
            <option value="doom-64">Doom 64</option>
            <option value="supabase">Supabase</option>
          </select>

          <script>
            (() => {
              const themeSelect = document.getElementById("theme-select");
              const storedTheme = localStorage.getItem("themeVariant");

              if (themeSelect && storedTheme) themeSelect.value = storedTheme;

              themeSelect.addEventListener("change", () => {
                const newTheme = themeSelect.value;

                document.documentElement.classList.forEach((c) => {
                  if (c.startsWith("theme-")) {
                    document.documentElement.classList.remove(c);
                  }
                });

                if (newTheme) {
                  document.documentElement.classList.add(`theme-${newTheme}`);
                  localStorage.setItem("themeVariant", newTheme);
                } else {
                  localStorage.removeItem("themeVariant");
                }
              });
            })();
          </script>

          <button
            type="button"
            aria-label="Toggle dark mode"
            data-tooltip="Toggle dark mode"
            data-side="bottom"
            onclick="document.dispatchEvent(new CustomEvent('basecoat:theme'))"
            class="btn-icon-outline size-8"
          >
            <span class="hidden dark:block">
              <svg
                xmlns="http://www.w3.org/2000/svg"
                width="20"
                height="20"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2"
                stroke-linecap="round"
                stroke-linejoin="round"
              >
                <circle cx="12" cy="12" r="4" />
                <path d="M12 2v2" />
                <path d="M12 20v2" />
                <path d="m4.93 4.93 1.41 1.41" />
                <path d="m17.66 17.66 1.41 1.41" />
                <path d="M2 12h2" />
                <path d="M20 12h2" />
                <path d="m6.34 17.66-1.41 1.41" />
                <path d="m19.07 4.93-1.41 1.41" />
              </svg>
            </span>

            <span class="block dark:hidden">
              <svg
                xmlns="http://www.w3.org/2000/svg"
                width="20"
                height="20"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2"
                stroke-linecap="round"
                stroke-linejoin="round"
              >
                <path d="M12 3a6 6 0 0 0 9 9 9 9 0 1 1-9-9Z" />
              </svg>
            </span>
          </button>

          <a href="/logout" class="btn-sm-outline">Logout</a>
        </div>
      </header>

      <div class="p-4 md:p-6 xl:p-12">
        <div class="dashboard-wrapper">
          <header class="dashboard-header">
            <h1 class="dashboard-title">
              Dashboard Reminder, Check-in & Attendance
            </h1>

            <p class="dashboard-subtitle text-muted-foreground">
              Selamat datang,
              <strong class="text-foreground">
                <%= typeof user !== "undefined" && user ? user : "Admins" %>
              </strong>.
              Halaman ini berfokus pada fitur dalam FTI Event Management:
              pengiriman reminder event, validasi check-in peserta, pencatatan
              attendance, rekap kehadiran, export Excel, dan REST API.
            </p>
          </header>

          <section class="dashboard-grid">
            <div class="card dashboard-card">
              <header>
                <h2>Kirim Reminder</h2>
                <p class="text-muted-foreground">
                  Kirim reminder kepada peserta yang sudah terdaftar pada event
                  yang berstatus published.
                </p>
              </header>

              <footer class="card-footer-actions">
                <a href="/reminders" class="btn">Buka Reminder</a>
              </footer>
            </div>

            <div class="card dashboard-card">
              <header>
                <h2>Riwayat Reminder</h2>
                <p class="text-muted-foreground">
                  Lihat daftar reminder yang sudah dikirim, termasuk event,
                  channel, pesan, pengirim, dan waktu pengiriman.
                </p>
              </header>

              <footer class="card-footer-actions">
                <a href="/reminders/history" class="btn">Lihat Riwayat</a>
              </footer>
            </div>

            <div class="card dashboard-card">
              <header>
                <h2>Check-in Peserta</h2>
                <p class="text-muted-foreground">
                  Validasi peserta berdasarkan ticket number dari e-ticket dan
                  simpan status kehadiran ke database.
                </p>
              </header>

              <footer class="card-footer-actions">
                <a href="/checkins" class="btn">Buka Check-in</a>
              </footer>
            </div>

            <div class="card dashboard-card">
              <header>
                <h2>Attendance List</h2>
                <p class="text-muted-foreground">
                  Tampilkan daftar peserta yang sudah melakukan check-in beserta
                  event, tiket, metode, dan status kehadiran.
                </p>
              </header>

              <footer class="card-footer-actions">
                <a href="/attendances" class="btn">Lihat Attendance</a>
              </footer>
            </div>

            <div class="card dashboard-card">
              <header>
                <h2>Rekap Attendance</h2>
                <p class="text-muted-foreground">
                  Lihat rekap total peserta terdaftar, peserta hadir, partial,
                  no show, dan peserta yang belum check-in.
                </p>
              </header>

              <footer class="card-footer-actions">
                <a href="/attendances/recap" class="btn">Lihat Rekap</a>
              </footer>
            </div>

            <div class="card dashboard-card">
              <header>
                <h2>Export Attendance Excel</h2>
                <p class="text-muted-foreground">
                  Unduh data attendance dalam bentuk file Excel untuk kebutuhan
                  rekap, dokumentasi, dan laporan kegiatan.
                </p>
              </header>

              <footer class="card-footer-actions">
                <a href="/attendances/export/excel" class="btn">Download Excel</a>
              </footer>
            </div>
          </section>

          <section class="card flow-card">
            <header>
              <h2>Alur Testing dari Dashboard</h2>
              <p class="text-muted-foreground">
                Semua fitur dites dari sidebar atau card pada halaman ini.
              </p>
            </header>

            <section class="flow-list">
              <div class="flow-item">
                <span class="flow-number bg-primary text-primary-foreground">1</span>
                <span>
                  Klik <strong>Kirim Reminder</strong>, pilih event, pilih
                  channel, isi pesan, lalu kirim reminder.
                </span>
              </div>

              <div class="flow-item">
                <span class="flow-number bg-primary text-primary-foreground">2</span>
                <span>
                  Klik <strong>Riwayat Reminder</strong> untuk memastikan reminder
                  yang dikirim sudah tersimpan.
                </span>
              </div>

              <div class="flow-item">
                <span class="flow-number bg-primary text-primary-foreground">3</span>
                <span>
                  Klik <strong>Check-in Peserta</strong>, lalu masukkan ticket
                  number peserta, misalnya <strong>TICKET-AKRAM-001</strong>.
                </span>
              </div>

              <div class="flow-item">
                <span class="flow-number bg-primary text-primary-foreground">4</span>
                <span>
                  Klik <strong>Attendance List</strong> untuk memastikan peserta
                  yang check-in muncul dalam daftar kehadiran.
                </span>
              </div>

              <div class="flow-item">
                <span class="flow-number bg-primary text-primary-foreground">5</span>
                <span>
                  Klik <strong>Rekap Attendance</strong> untuk melihat perubahan
                  total hadir dan belum check-in.
                </span>
              </div>

              <div class="flow-item">
                <span class="flow-number bg-primary text-primary-foreground">6</span>
                <span>
                  Klik <strong>Export Attendance Excel</strong> untuk mengunduh
                  file rekap attendance dalam format Excel.
                </span>
              </div>

              <div class="flow-item">
                <span class="flow-number bg-primary text-primary-foreground">7</span>
                <span>
                  Klik menu <strong>API</strong> untuk memastikan REST API
                  menampilkan data dalam format JSON.
                </span>
              </div>
            </section>
          </section>
        </div>
      </div>
    </main>

    <div id="toaster" class="toaster"></div>
  </body>
</html>