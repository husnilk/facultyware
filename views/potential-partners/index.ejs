<!doctype html>
<html lang="id">
  <head>
    <script>
      (() => {
        try {
          const s = localStorage.getItem('themeMode');
          if (s ? s === 'dark' : matchMedia('(prefers-color-scheme: dark)').matches)
            document.documentElement.classList.add('dark');
        } catch(_) {}
        document.addEventListener('basecoat:theme', e => {
          const m = e.detail?.mode;
          const dark = m === 'dark' ? true : m === 'light' ? false : !document.documentElement.classList.contains('dark');
          document.documentElement.classList.toggle('dark', dark);
          try { localStorage.setItem('themeMode', dark ? 'dark' : 'light'); } catch(_) {}
        });
      })();
    </script>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= title %> - Facultyware</title>
    <meta name="description" content="Kelola data mitra potensial fakultas dengan mudah dan terstruktur." />
    <link rel="stylesheet" href="/assets/styles.css?v=2" />
    <script src="https://unpkg.com/htmx.org@2.0.4" defer></script>
  </head>
  <body>
    <!-- ===== SIDEBAR ===== -->
    <aside id="sidebar" class="sidebar" data-side="left">
      <nav aria-label="Sidebar navigation">
        <header>
          <a href="/home" class="btn-ghost p-2 h-12 w-full justify-start">
            <div class="bg-sidebar-primary text-sidebar-primary-foreground flex aspect-square size-8 items-center justify-center rounded-lg">
              <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" class="h-4 w-4"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg>
            </div>
            <div class="grid flex-1 text-left text-sm leading-tight ml-2">
              <span class="truncate font-semibold">Facultyware</span>
              <span class="truncate text-xs opacity-60">Sistem Informasi Fakultas</span>
            </div>
          </a>
        </header>
        <section>
          <div role="group" aria-labelledby="nav-main">
            <h3 id="nav-main">Menu Utama</h3>
            <ul>
              <li>
                <a href="/home">
                  <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="m3 9 9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/><polyline points="9 22 9 12 15 12 15 22"/></svg>
                  <span>Dashboard</span>
                </a>
              </li>
              <li>
                <a href="/potential-partners" aria-current="page">
                  <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect width="20" height="14" x="2" y="7" rx="2" ry="2"/><path d="M16 21V5a2 2 0 0 0-2-2h-4a2 2 0 0 0-2 2v16"/></svg>
                  <span>Potential Partner</span>
                </a>
              </li>
              <li>
                <a href="/mou">
                  <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/><polyline points="10 9 9 9 8 9"/></svg>
                  <span>Draft MoU</span>
                </a>
              </li>
              <li>
                <a href="/follow-up">
                  <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 20h9"/><path d="M16.5 3.5a2.121 2.121 0 0 1 3 3L7 19l-4 1 1-4L16.5 3.5z"/></svg>
                  <span>Follow Up</span>
                </a>
              </li>
            </ul>
          </div>
        </section>
        <footer>
          <div class="px-3 py-2">
            <div class="flex items-center gap-3 rounded-md px-2 py-2 text-sm">
              <div class="flex aspect-square size-8 items-center justify-center rounded-full bg-primary text-primary-foreground text-xs font-bold">
                <%= user ? user.charAt(0).toUpperCase() : 'A' %>
              </div>
              <div class="grid flex-1 text-left leading-tight">
                <span class="truncate text-sm font-medium"><%= user || 'Admin' %></span>
                <span class="truncate text-xs opacity-60">Administrator</span>
              </div>
            </div>
            <a href="/logout" class="btn btn-ghost w-full justify-start mt-1 text-sm gap-2">
              <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"/><polyline points="16 17 21 12 16 7"/><line x1="21" x2="9" y1="12" y2="12"/></svg>
              Keluar
            </a>
          </div>
        </footer>
      </nav>
    </aside>

    <!-- ===== MAIN CONTENT ===== -->
    <main class="peer-[.sidebar]/sidebar:ml-64 flex-1 flex flex-col min-h-screen" style="margin-left:var(--sidebar-width,16rem)">
      <!-- Topbar -->
      <header class="border-b bg-background sticky top-0 z-10">
        <div class="flex h-14 items-center gap-4 px-6">
          <nav aria-label="Breadcrumb" class="flex items-center gap-2 text-sm text-muted-foreground">
            <a href="/home" class="hover:text-foreground">Dashboard</a>
            <span>/</span>
            <span class="text-foreground font-medium">Potential Partner</span>
          </nav>
        </div>
      </header>

      <!-- Page Content -->
      <div class="flex-1 p-6 space-y-6">

        <!-- Page Header -->
        <div class="flex items-center justify-between">
          <div>
            <h1 class="text-2xl font-bold tracking-tight">Data Potential Partner</h1>
            <p class="text-sm text-muted-foreground mt-1">Kelola data calon mitra potensial fakultas</p>
          </div>
          <div class="flex items-center gap-3">
            <!-- Dropdown Unduh Laporan -->
            <div class="relative inline-block text-left" id="export-dropdown">
              <button type="button" 
                      id="btn-export-dropdown"
                      style="display:inline-flex;align-items:center;gap:8px;padding:6px 14px;border-radius:6px;border:1px solid #52525b;background:transparent;color:inherit;font-size:0.875rem;font-weight:500;cursor:pointer;white-space:nowrap;"
                      onclick="document.getElementById('dropdown-options').classList.toggle('hidden')">
                <svg xmlns="http://www.w3.org/2000/svg" width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" x2="12" y1="15" y2="3"/></svg>
                Unduh Laporan
                <svg xmlns="http://www.w3.org/2000/svg" width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="m6 9 6 6 6-6"/></svg>
              </button>
              <div id="dropdown-options" 
                   class="hidden absolute right-0 top-full mt-2 w-48 rounded-md border border-zinc-200 dark:border-zinc-800 bg-white dark:bg-zinc-900 text-zinc-900 dark:text-zinc-50 shadow-lg z-50 focus:outline-none py-1">
                <a href="/potential-partners/export/excel" class="flex items-center px-4 py-2.5 text-sm text-zinc-700 dark:text-zinc-200 hover:bg-zinc-100 dark:hover:bg-zinc-800 transition-colors gap-2">
                  <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" class="text-emerald-600 dark:text-emerald-500"><rect x="3" y="3" width="18" height="18" rx="2" ry="2"/><line x1="9" y1="9" x2="15" y2="15"/><line x1="15" y1="9" x2="9" y2="15"/></svg>
                  Ekspor Excel (.xlsx)
                </a>
                <a href="/potential-partners/export/pdf" class="flex items-center px-4 py-2.5 text-sm text-zinc-700 dark:text-zinc-200 hover:bg-zinc-100 dark:hover:bg-zinc-800 transition-colors gap-2">
                  <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" class="text-rose-500 dark:text-rose-400"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/><polyline points="10 9 9 9 8 9"/></svg>
                  Ekspor PDF (.pdf)
                </a>
                <div class="h-px bg-zinc-200 dark:bg-zinc-800 my-1"></div>
                <a href="/potential-partners/export/csv" class="flex items-center px-4 py-2.5 text-sm text-zinc-700 dark:text-zinc-200 hover:bg-zinc-100 dark:hover:bg-zinc-800 transition-colors gap-2">
                  <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" class="text-sky-500 dark:text-sky-400"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" x2="12" y1="15" y2="3"/></svg>
                  Ekspor CSV (.csv)
                </a>
              </div>
            </div>

            <!-- Tombol Tambah Partner -->
            <a href="/potential-partners/add" id="btn-tambah-partner"
               class="btn btn-primary gap-2">
              <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14"/><path d="M12 5v14"/></svg>
              Tambah Partner
            </a>
          </div>
        </div>

        <script>
          document.addEventListener('click', function(e) {
            const dropdown = document.getElementById('export-dropdown');
            const options = document.getElementById('dropdown-options');
            if (dropdown && options && !dropdown.contains(e.target)) {
              options.classList.add('hidden');
            }
          });
        </script>

        <!-- Flash Messages -->
        <% if (messages && messages.success) { %>
        <div class="alert alert-success flex items-center gap-3" role="alert">
          <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><polyline points="22 4 12 14.01 9 11.01"/></svg>
          <span><%= messages.success %></span>
        </div>
        <% } %>
        <% if (messages && messages.error) { %>
        <div class="alert alert-destructive flex items-center gap-3" role="alert">
          <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><line x1="12" x2="12" y1="8" y2="12"/><line x1="12" x2="12.01" y1="16" y2="16"/></svg>
          <span><%= messages.error %></span>
        </div>
        <% } %>

        <!-- Search & Filter Bar -->
        <div class="card">
          <div class="card-content pt-4">
            <form method="GET" action="/potential-partners" id="form-search" class="flex flex-wrap gap-3 items-end">
              <div class="field flex-1 min-w-48">
                <label class="label" for="search">Cari</label>
                <div style="position:relative;display:flex;align-items:center;">
                  <svg xmlns="http://www.w3.org/2000/svg" width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="position:absolute;left:10px;pointer-events:none;opacity:0.5;"><circle cx="11" cy="11" r="8"/><path d="m21 21-4.3-4.3"/></svg>
                  <input type="text" id="search" name="search" class="input"
                    style="padding-left:34px;width:100%;"
                    placeholder="Nama perusahaan, email, telepon..."
                    value="<%= search %>" />
                </div>
              </div>
              <div class="field w-44">
                <label class="label" for="status-filter">Status</label>
                <select id="status-filter" name="status" class="input w-full">
                  <option value="">Semua Status</option>
                  <option value="identified"    <%= filterStatus === 'identified'    ? 'selected' : '' %>>Teridentifikasi</option>
                  <option value="converted"     <%= filterStatus === 'converted'     ? 'selected' : '' %>>Kerjasama</option>
                  <option value="rejected"      <%= filterStatus === 'rejected'      ? 'selected' : '' %>>Ditolak</option>
                </select>
              </div>
              <div class="flex gap-2">
                <button type="submit" id="btn-cari" class="btn btn-primary">Cari</button>
                <% if (search || filterStatus) { %>
                <a href="/potential-partners" class="btn btn-outline">Reset</a>
                <% } %>
              </div>
            </form>
          </div>
        </div>

        <!-- Stats badges -->
        <div class="flex items-center gap-3 text-sm text-muted-foreground">
          <span>Menampilkan <strong class="text-foreground"><%= potentialPartners.length %></strong> dari <strong class="text-foreground"><%= total %></strong> data</span>
          <% if (search || filterStatus) { %>
            <span class="badge badge-outline">Filter aktif</span>
          <% } %>
        </div>

        <!-- Table Card -->
        <div class="card">
          <div class="overflow-x-auto">
            <table class="table w-full">
              <thead>
                <tr>
                  <th class="w-10">#</th>
                  <th>Nama Perusahaan</th>
                  <th>Contact Person</th>
                  <th>Email</th>
                  <th>Telepon</th>
                  <th>Tipe Partnership</th>
                  <th>Status</th>
                  <th class="text-center w-32">Aksi</th>
                </tr>
              </thead>
              <tbody>
                <% if (potentialPartners && potentialPartners.length > 0) { %>
                  <% potentialPartners.forEach((partner, index) => { %>
                  <tr>
                    <td class="text-muted-foreground text-sm"><%= ((currentPage - 1) * 10) + index + 1 %></td>
                    <td>
                      <div class="font-medium"><%= partner.company_name %></div>
                      <% if (partner.address) { %>
                        <div class="text-xs text-muted-foreground mt-0.5 truncate max-w-48" title="<%= partner.address %>"><%= partner.address %></div>
                      <% } %>
                    </td>
                    <td class="text-sm"><%= partner.contact_person || '<span class="text-muted-foreground">-</span>' %></td>
                    <td class="text-sm">
                      <% if (partner.email) { %>
                        <a href="mailto:<%= partner.email %>" class="hover:underline text-primary"><%= partner.email %></a>
                      <% } else { %>
                        <span class="text-muted-foreground">-</span>
                      <% } %>
                    </td>
                    <td class="text-sm"><%= partner.phone || '<span class="text-muted-foreground">-</span>' %></td>
                    <td>
                      <% if (partner.partnership_type) { %>
                        <% const typeColors = {university:'badge-info', company:'badge-warning', government:'badge-secondary', ngo:'badge-success', other:'badge-outline'};
                           const typeLabels = {university:'Universitas', company:'Perusahaan', government:'Pemerintahan', ngo:'NGO', other:'Lainnya'}; %>
                        <span class="badge <%= typeColors[partner.partnership_type] || 'badge-outline' %> text-xs">
                          <%= typeLabels[partner.partnership_type] || partner.partnership_type %>
                        </span>
                      <% } else { %>
                        <span class="text-muted-foreground text-sm">-</span>
                      <% } %>
                    </td>
                    <td>
                      <% const statusBadge = {identified:'badge-info', in_discussion:'badge-warning', proposed:'badge-secondary', converted:'badge-success', rejected:'badge-destructive'};
                         const statusLabel = {identified:'Teridentifikasi', in_discussion:'Diskusi', proposed:'Diajukan', converted:'Kerjasama', rejected:'Ditolak'}; %>
                      <span class="badge <%= statusBadge[partner.status] || 'badge-outline' %> text-xs">
                        <%= statusLabel[partner.status] || partner.status %>
                      </span>
                    </td>
                    <td>
                      <div class="flex items-center justify-center gap-1">
                        <% if (partner.status !== 'converted' && partner.status !== 'rejected') { %>
                        <form action="/potential-partners/approve/<%= partner.id %>" method="POST" class="inline">
                          <button type="submit"
                                  id="btn-setujui-list-<%= partner.id %>"
                                  class="btn btn-sm btn-icon text-white"
                                  style="background-color: #16a34a; border-color: #16a34a;"
                                  title="Setujui Kerjasama">
                            <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="20 6 9 17 4 12"/></svg>
                          </button>
                        </form>
                        <form action="/potential-partners/reject/<%= partner.id %>" method="POST" class="inline">
                          <button type="submit"
                                  id="btn-tolak-list-<%= partner.id %>"
                                  class="btn btn-sm btn-icon text-white"
                                  style="background-color: #d97706; border-color: #d97706;"
                                  title="Tolak Kerjasama">
                            <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
                          </button>
                        </form>
                        <% } %>

                        <a href="/potential-partners/detail/<%= partner.id %>"
                           id="btn-detail-<%= partner.id %>"
                           class="btn btn-outline btn-sm btn-icon"
                           title="Lihat detail">
                          <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M2.062 12.348a1 1 0 0 1 0-.696 10.75 10.75 0 0 1 19.876 0 1 1 0 0 1 0 .696 10.75 10.75 0 0 1-19.876 0z"/><circle cx="12" cy="12" r="3"/></svg>
                        </a>

                        <a href="/potential-partners/edit/<%= partner.id %>"
                           id="btn-edit-<%= partner.id %>"
                           class="btn btn-outline btn-sm btn-icon"
                           title="Edit data">
                          <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M17 3a2.85 2.83 0 1 1 4 4L7.5 20.5 2 22l1.5-5.5Z"/></svg>
                        </a>
                        <form action="/potential-partners/delete/<%= partner.id %>" method="POST" class="inline"
                               onsubmit="return confirm('Yakin ingin menghapus data \u0022<%= partner.company_name.replace(/'/g, `\\'`) %>\u0022?\nData yang dihapus tidak dapat dikembalikan.')">
                          <button type="submit"
                                  id="btn-hapus-<%= partner.id %>"
                                  class="btn btn-destructive btn-sm btn-icon"
                                  title="Hapus data">
                            <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M3 6h18"/><path d="M19 6v14c0 1-1 2-2 2H7c-1 0-2-1-2-2V6"/><path d="M8 6V4c0-1 1-2 2-2h4c1 0 2 1 2 2v2"/></svg>
                          </button>
                        </form>
                      </div>
                    </td>
                  </tr>
                  <% }) %>
                <% } else { %>
                  <tr>
                    <td colspan="8" class="py-16 text-center">
                      <div class="flex flex-col items-center gap-3 text-muted-foreground">
                        <svg xmlns="http://www.w3.org/2000/svg" width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><rect width="20" height="14" x="2" y="7" rx="2"/><path d="M16 21V5a2 2 0 0 0-2-2h-4a2 2 0 0 0-2 2v16"/></svg>
                        <div>
                          <p class="font-medium">
                            <% if (search || filterStatus) { %>Data tidak ditemukan<% } else { %>Belum ada data potential partner<% } %>
                          </p>
                          <p class="text-sm mt-1">
                            <% if (search || filterStatus) { %>Coba ubah kata kunci pencarian atau hapus filter<% } else { %>Klik tombol "Tambah Partner" untuk menambahkan data pertama<% } %>
                          </p>
                        </div>
                        <% if (!search && !filterStatus) { %>
                        <a href="/potential-partners/add" class="btn btn-primary btn-sm gap-2">
                          <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14"/><path d="M12 5v14"/></svg>
                          Tambah Sekarang
                        </a>
                        <% } %>
                      </div>
                    </td>
                  </tr>
                <% } %>
              </tbody>
            </table>
          </div>

          <!-- Pagination -->
          <% if (totalPages > 1) { %>
          <div class="border-t px-4 py-3 flex items-center justify-between">
            <p class="text-sm text-muted-foreground">
              Halaman <strong><%= currentPage %></strong> dari <strong><%= totalPages %></strong>
            </p>
            <nav aria-label="Pagination" class="pagination">
              <% const buildUrl = (p) => {
                const params = new URLSearchParams();
                if (search) params.set('search', search);
                if (filterStatus) params.set('status', filterStatus);
                params.set('page', p);
                return '/potential-partners?' + params.toString();
              } %>

              <a href="<%= buildUrl(1) %>" class="pagination-item <%= currentPage === 1 ? 'disabled' : '' %>" aria-label="First">«</a>
              <a href="<%= buildUrl(Math.max(1, currentPage - 1)) %>" class="pagination-item <%= currentPage === 1 ? 'disabled' : '' %>" aria-label="Previous">‹</a>

              <% for (let p = Math.max(1, currentPage - 2); p <= Math.min(totalPages, currentPage + 2); p++) { %>
                <a href="<%= buildUrl(p) %>" class="pagination-item <%= p === currentPage ? 'pagination-item-active' : '' %>"><%= p %></a>
              <% } %>

              <a href="<%= buildUrl(Math.min(totalPages, currentPage + 1)) %>" class="pagination-item <%= currentPage === totalPages ? 'disabled' : '' %>" aria-label="Next">›</a>
              <a href="<%= buildUrl(totalPages) %>" class="pagination-item <%= currentPage === totalPages ? 'disabled' : '' %>" aria-label="Last">»</a>
            </nav>
          </div>
          <% } %>
        </div>


      </div><!-- /page content -->
    </main>

    <script src="/assets/js/basecoat.js" defer></script>
  </body>
</html>
