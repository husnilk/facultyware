<%# Header — FacultyWare %>
<header class="app-header">
  <div class="fw-header-inner">
    <h2 class="fw-header-title"><%= typeof title !== 'undefined' ? title : 'FacultyWare' %></h2>

    <% if (user) { %>
      <div class="fw-header-user">
        <div class="fw-avatar"><%= user.name.charAt(0).toUpperCase() %></div>
        <div class="fw-header-user-info">
          <div class="fw-header-user-name"><%= user.name %></div>
          <div class="fw-header-user-role"><%= user.role_name %></div>
        </div>
        <a href="/logout" class="fw-header-logout" title="Logout">
          <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"/>
            <polyline points="16 17 21 12 16 7"/>
            <line x1="21" y1="12" x2="9" y2="12"/>
          </svg>
        </a>
      </div>
    <% } %>
  </div>
</header>
